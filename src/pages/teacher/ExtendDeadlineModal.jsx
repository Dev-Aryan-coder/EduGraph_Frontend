import React from 'react'
import './ExtendDeadlineModal.css'

export default function ExtendDeadlineModal() {
  return (
    <div className="extendDeadlineModal-container">
      {/* Deadline extension modal (max 48 hours + reason) */}
    </div>
  )
}